<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">
    



    <fragment
        android:id="@+id/navigation_home"
        android:name="it.unimib.travelnotes.ui.flight.FlightFragment"
        android:label="@string/volo"
        tools:layout="@layout/fragment_flight" >
        <action
            android:id="@+id/action_navigation_home_to_navigation_dashboard"
            app:destination="@id/navigation_dashboard" />
    </fragment>

    <fragment
        android:id="@+id/navigation_dashboard"
        android:name="it.unimib.travelnotes.ui.attivita.AttivitaFragment"
        android:label="@string/attivita"
        tools:layout="@layout/fragment_attivita" >
        <action
            android:id="@+id/action_navigation_dashboard_to_navigation_notifications"
            app:destination="@id/navigation_notifications" />
    </fragment>

    <fragment
        android:id="@+id/navigation_notifications"
        android:name="it.unimib.travelnotes.ui.group.GroupFragment"
        android:label="@string/gruppo"
        tools:layout="@layout/fragment_group" />
</navigation>